<Window x:Class="ETWSpyUI.ProviderListEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ETWSpyUI"
        mc:Ignorable="d"
        Style="{StaticResource ThemedWindow}"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Title="ETWSpy - Edit Provider List" 
        Height="550" 
        Width="700"
        MinHeight="450"
        MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Closing="Window_Closing">
    <Grid Background="{DynamicResource SecondaryBackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input Form -->
        <Grid Grid.Row="0" Background="{DynamicResource SecondaryBackgroundBrush}" Margin="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Provider Name:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="2"/>
            <TextBox Grid.Row="0" Grid.Column="1" 
                     x:Name="ProviderNameTextBox" 
                     Height="26" 
                     Margin="2" 
                     VerticalAlignment="Center"
                     TextChanged="InputFields_TextChanged"
                     ToolTip="Enter the provider name (e.g., Microsoft-Windows-Kernel-Process)"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Provider GUID:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="2"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                     x:Name="ProviderGuidTextBox" 
                     Height="26" 
                     Margin="2" 
                     VerticalAlignment="Center"
                     TextChanged="InputFields_TextChanged"
                     ToolTip="Enter the provider GUID (e.g., {22fb2cd6-0e7b-422b-a0c7-2fad1fd0e716})"/>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,10">
            <Button Content="Add" 
                    x:Name="AddButton" 
                    Click="AddButton_Click" 
                    Margin="5,0" 
                    MinWidth="100" 
                    IsEnabled="False"
                    ToolTip="Add a new provider to the list"/>
            <Button Content="Edit" 
                    x:Name="EditButton" 
                    Click="EditButton_Click" 
                    Margin="5,0" 
                    MinWidth="100" 
                    IsEnabled="False"
                    ToolTip="Edit the selected provider"/>
            <Button Content="Remove" 
                    x:Name="RemoveButton" 
                    Click="RemoveButton_Click" 
                    Margin="5,0" 
                    MinWidth="100" 
                    IsEnabled="False"
                    ToolTip="Remove selected provider(s) from the list"/>
        </StackPanel>

        <!-- Provider List -->
        <Grid Grid.Row="2">
            <ListView x:Name="ProvidersListView"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      Margin="0"
                      SelectionChanged="ProvidersListView_SelectionChanged"
                      MouseDoubleClick="ProvidersListView_MouseDoubleClick"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      SelectionMode="Extended">
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem x:Name="ContextMenuEditMenuItem" Header="_Edit" Click="EditButton_Click" IsEnabled="False">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="ContextMenuRemoveMenuItem" Header="_Remove" Click="RemoveButton_Click" IsEnabled="False">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn Header="Provider Name" Width="350" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="GUID" Width="280" DisplayMemberBinding="{Binding GuidDisplay}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            
            <!-- Empty state placeholder -->
            <TextBlock x:Name="EmptyPlaceholderText"
                       Text="No providers in the list.&#x0a;&#x0a;Add providers using the fields above."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                       FontSize="14"
                       Margin="20"
                       Visibility="Collapsed"
                       IsHitTestVisible="False"/>
        </Grid>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Reset to Default" 
                    x:Name="ResetButton" 
                    Click="ResetButton_Click" 
                    Margin="5,0" 
                    MinWidth="120"
                    ToolTip="Reset provider list to the built-in defaults"/>
            <Button Content="Save" 
                    x:Name="SaveButton" 
                    Click="SaveButton_Click" 
                    Margin="5,0" 
                    MinWidth="100"
                    ToolTip="Save changes to the provider list file"/>
            <Button Content="Cancel" 
                    x:Name="CancelButton" 
                    Click="CancelButton_Click" 
                    Margin="5,0" 
                    MinWidth="100"
                    ToolTip="Cancel and discard changes"/>
        </StackPanel>
    </Grid>
</Window>
