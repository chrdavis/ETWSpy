<Window x:Class="ETWSpyUI.EventDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ETWSpyUI"
        mc:Ignorable="d"
        Style="{StaticResource ThemedWindow}"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Title="ETWSpy - Event Details" 
        Height="550" 
        Width="400"
        MinHeight="500"
        MinWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <TabControl Grid.Row="0" Margin="10">
            <TabItem Header="Event">
                <Grid Background="{DynamicResource SecondaryBackgroundBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Background="{DynamicResource SecondaryBackgroundBrush}" Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.Column="0" Content="Provider:" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="0" Grid.Column="1" Content="(Provider)" x:Name="ProviderLabel" FontSize="12"  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="1" Grid.Column="0" Content="Event Id:" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="1" Grid.Column="1" Content="(Id)" x:Name="EventIdLabel" FontSize="12"  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="2" Grid.Column="0" Content="Timestamp:" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="2" Grid.Column="1" Content="(Id)" x:Name="TimestampdLabel" FontSize="12"  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="3" Grid.Column="0" Content="Event Name:" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="3" Grid.Column="1" Content="(EventName)" x:Name="EventNameLabel" FontSize="12"  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="4" Grid.Column="0" Content="Task Name:" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                        <Label Grid.Row="4" Grid.Column="1" Content="(TaskName)" x:Name="TaskNameLabel" FontSize="12"  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4"/>
                    </Grid>
                    <ListView
                      Grid.Row="1"
                      x:Name="EventPayloadListView"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      Margin="0"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListView.View>
                            <GridView AllowsColumnReorder="True">
                                <GridViewColumn Header="Name" Width="150" DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Header="Type" Width="100" DisplayMemberBinding="{Binding Type}"/>
                                <GridViewColumn Header="Value" Width="150" DisplayMemberBinding="{Binding Value}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>

            </TabItem>
        </TabControl>
        <Button Grid.Row="1" Content="Close" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Click="CloseButton_Click" MinWidth="100"/>
    </Grid> 
</Window>
