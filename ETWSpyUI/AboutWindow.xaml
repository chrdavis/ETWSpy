<Window x:Class="ETWSpyUI.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ETWSpyUI"
        mc:Ignorable="d"
        Style="{StaticResource ThemedWindow}"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Title="ETWSpy - About" 
        Height="180" 
        Width="400"
        MinHeight="180"
        MinWidth="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Background="{DynamicResource SecondaryBackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="ETWSpy - Event Tracing Monitor for Windows" HorizontalAlignment="Left" Margin="10" VerticalAlignment="Top"/>
        <Label Grid.Row="1" x:Name="VersionLabel" Content="Version: Unknown" HorizontalAlignment="Left" Margin="10" VerticalAlignment="Top"/>
        <TextBlock Grid.Row="2" HorizontalAlignment="Left" Margin="14,10,10,10" VerticalAlignment="Top">
            <Hyperlink NavigateUri="https://github.com/chrdavis/ETWSpy" RequestNavigate="Hyperlink_RequestNavigate">
                https://github.com/chrdavis/ETWSpy
            </Hyperlink>
        </TextBlock>

        <Button Grid.Row="4" Content="Close" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Click="CloseButton_Click" MinWidth="100"/>
    </Grid>
</Window>
