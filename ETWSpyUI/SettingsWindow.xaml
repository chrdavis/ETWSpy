<Window x:Class="ETWSpyUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ETWSpyUI"
        mc:Ignorable="d"
        Style="{StaticResource ThemedWindow}"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Title="ETWSpy - Settings" 
        Height="200" 
        Width="450"
        MinHeight="200"
        MinWidth="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Background="{DynamicResource SecondaryBackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Dark Mode:" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4"/>
        <CheckBox Grid.Row="0" Grid.Column="1" x:Name="DarkModeCheckBox" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4" Checked="DarkModeCheckBox_Changed" Unchecked="DarkModeCheckBox_Changed"/>
        
        <Label Grid.Row="1" Grid.Column="0" Content="Show Timestamps in UTC:" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4"/>
        <CheckBox Grid.Row="1" Grid.Column="1" x:Name="UTCCheckBox" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4" Checked="UTCCheckBox_Changed" Unchecked="UTCCheckBox_Changed"/>
        
        <Label Grid.Row="2" Grid.Column="0" Content="Max Events to Show:" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="4">
            <Slider x:Name="MaxEventsSlider" 
                    Minimum="1000" 
                    Maximum="100000" 
                    VerticalAlignment="Center" 
                    Width="200"
                    TickFrequency="10000"
                    IsSnapToTickEnabled="False"
                    SmallChange="1000"
                    LargeChange="10000"
                    ValueChanged="MaxEventsSlider_ValueChanged"/>
            <TextBlock x:Name="MaxEventsText"
                       VerticalAlignment="Center" 
                       Margin="10,0,0,0"
                       MinWidth="60"/>
        </StackPanel>

        <Button Grid.Row="4" Grid.Column="1" Content="Close" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="4" Click="CloseButton_Click" MinWidth="100"/>
    </Grid>
</Window>
