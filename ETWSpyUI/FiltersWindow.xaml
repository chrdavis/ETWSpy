<Window x:Class="ETWSpyUI.FiltersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ETWSpyUI"
        mc:Ignorable="d"
        Style="{StaticResource ThemedWindow}"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Title="ETWSpy - Filters" 
        Height="500" 
        Width="900"
        MinHeight="400"
        MinWidth="700">
    <Grid Background="{DynamicResource SecondaryBackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- form -->
        <Grid Grid.Row="0" Background="{DynamicResource SecondaryBackgroundBrush}" Margin="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Provider:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="2"/>
            <ComboBox Grid.Row="0"
                      Grid.Column="1"
                      x:Name="ProviderComboBox"
                      Margin="2"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"
                      Width="400"
                      IsEditable="False"
                      IsReadOnly="True"
                      IsTextSearchEnabled="True"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Filter Type:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="2"/>
            <ComboBox Grid.Row="1"
                      Grid.Column="1"
                      x:Name="FilterCategoryComboBox"
                      Margin="2"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"
                      Width="150"
                      SelectionChanged="FilterCategoryComboBox_SelectionChanged"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Value:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="2"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="ValueTextBox" Height="26" Margin="2" HorizontalAlignment="Left" VerticalAlignment="Center" Width="400"/>
            <Label Grid.Row="2" Grid.Column="2" x:Name="ValueInstructionLabel" Content="(Separate IDs with ','; Specify range with '-')" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Action:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="2"/>
            <ComboBox Grid.Row="3" Grid.Column="1" x:Name="ActionComboBox" Margin="2" HorizontalAlignment="Left" VerticalAlignment="Center" Width="150"/>
        </Grid>

        <!-- buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,10">
            <Button Content="Add" x:Name="AddFilterButton" Click="AddFilter_Click" Margin="5,0" MinWidth="100"/>
            <Button Content="Remove" x:Name="RemoveFilterButton" Click="RemoveFilter_Click" Margin="5,0" MinWidth="100" IsEnabled="False"/>
            <Button Content="Clear" x:Name="ClearFiltersButton" Click="ClearFilters_Click" Margin="5,0" MinWidth="100" IsEnabled="False"/>
        </StackPanel>

        <!-- list (fills remaining space) with empty state placeholder -->
        <Grid Grid.Row="2">
            <ListView x:Name="FiltersListView"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      Margin="0"
                      SelectionChanged="FiltersListView_SelectionChanged"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ListView.View>
                    <GridView AllowsColumnReorder="True">
                        <GridViewColumn Header="Provider" Width="300" DisplayMemberBinding="{Binding Provider}"/>
                        <GridViewColumn Header="Filter Type" Width="100" DisplayMemberBinding="{Binding FilterCategory}"/>
                        <GridViewColumn Header="Value" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="{Binding Value}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Value}" Value="">
                                                        <Setter Property="Text" Value="(All)"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Action" Width="100" DisplayMemberBinding="{Binding FilterLogic}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            
            <!-- Empty state placeholder -->
            <TextBlock x:Name="EmptyPlaceholderText"
                       Text="No additional filters configured.&#x0a;&#x0a;Select the provider(s) and filter(s) you would like to apply."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                       FontSize="14"
                       Margin="20"
                       IsHitTestVisible="False"/>
        </Grid>

        <!-- Close button -->
        <Button Grid.Row="3" Content="Close" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Click="CloseButton_Click" MinWidth="100"/>
    </Grid>
</Window>
